количество аргументов 
командной строки больше 1

подсчитать суммарную
длину аргументов 
командной строки

выделить память под
обрабатываемую строку

скопировать символы из
аргументов командной строки
в обрабатываемую строку

выделить память
под буфер ввода

ввести строку со
стандартного ввода

подсчитать количество
введенных символов

выделить память под
обрабатываемую строку

скопировать символы
из буфера ввода в 
обрабатываемую строку

освободить память
используемую для 
буфера ввода.

выводим обрабатываемую 
строку на стандартный вывод

***********************

установить указатель 
читаемого символа
на начало строки

установить указатель 
записываемого символа
на начало строки

установить счетчик символов в 1

текущий читаемый 
символ не конец строки

текущий читаемый символ 
равен следующему

увеличить счетчик символов на 1

счетчик символов не равен 1

по разрядно записываем строковое
преставление счетчика символов
в записываемый символ, увеличивая 
указатель после записи каждого разряда

копируем читаемый символ в записываемый

увеличиваем указатель записываемого символа на 1

увеличиваем указатель читаемого символа на 1

записываем в записываемый символ символ конца строки.

*****************

выводим получившуюся строку на стандартный вывод


освобождаем память используемую для хранения строки

конец




